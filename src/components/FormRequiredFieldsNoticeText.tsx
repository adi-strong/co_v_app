import {Form} from "react-bootstrap";

const FormRequiredFieldsNoticeText = () => {

  return <Form.Text as='p'>Veuillez renseigner les champs (<code>*</code>) obligatoires :</Form.Text>
  
};

export default FormRequiredFieldsNoticeText;
