// Service fonction à implémenter
import {createSlice, PayloadAction} from "@reduxjs/toolkit";

const fonctionSlice = createSlice({

  name: 'fonction',
  initialState: { },
  reducers: {

  },
});

export const { } = fonctionSlice.actions

export default fonctionSlice.reducer
