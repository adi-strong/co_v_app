// Service fonction à implémenter
import {API, API_PATH} from "../../../app/store";

const fonctionApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = fonctionApiSlice
