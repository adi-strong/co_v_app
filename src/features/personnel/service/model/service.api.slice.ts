// Service service à implémenter
import {API, API_PATH} from "../../../app/store";

const serviceApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = serviceApiSlice
