// Service departement à implémenter
import {createSlice, PayloadAction} from "@reduxjs/toolkit";

const departementSlice = createSlice({

  name: 'departement',
  initialState: { },
  reducers: {

  },
});

export const { } = departementSlice.actions

export default departementSlice.reducer
