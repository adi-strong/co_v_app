// Service departement à implémenter
import {API, API_PATH} from "../../../app/store";

const departementApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = departementApiSlice
