// Service compteCaisse à implémenter
import {API, API_PATH} from "../../../app/store";

const compteCaisseApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = compteCaisseApiSlice
