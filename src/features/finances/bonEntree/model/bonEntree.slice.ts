// Service bonEntree à implémenter
import {createSlice, PayloadAction} from "@reduxjs/toolkit";

const bonEntreeSlice = createSlice({

  name: 'bonEntree',
  initialState: { },
  reducers: {

  },
});

export const { } = bonEntreeSlice.actions

export default bonEntreeSlice.reducer
