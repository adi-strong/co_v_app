// Service bonDeDepenses à implémenter
import {createSlice, PayloadAction} from "@reduxjs/toolkit";

const bonDeDepensesSlice = createSlice({
  name: 'bonDeDepenses',
  initialState: { },
  reducers: {
  
  },
});

export const { } = bonDeDepensesSlice.actions;

export default bonDeDepensesSlice.reducer;
