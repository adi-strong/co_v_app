// Service typesDepenses à implémenter
import {createSlice, PayloadAction} from "@reduxjs/toolkit";

const typesDepensesSlice = createSlice({
  name: 'typesDepenses',
  initialState: { },
  reducers: {
  
  },
});

export const { } = typesDepensesSlice.actions;

export default typesDepensesSlice.reducer;
