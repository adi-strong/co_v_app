// Service structure à implémenter
import {API, API_PATH} from "../../../app/store";

const structureApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = structureApiSlice
