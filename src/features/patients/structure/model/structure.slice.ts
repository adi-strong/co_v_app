// Service structure à implémenter
import {createSlice, PayloadAction} from "@reduxjs/toolkit";

const structureSlice = createSlice({

  name: 'structure',
  initialState: { },
  reducers: {

  },
});

export const { } = structureSlice.actions

export default structureSlice.reducer
