// Service patient à implémenter
import {API, API_PATH} from "../../../app/store";

const patientApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = patientApiSlice
