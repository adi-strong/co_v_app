// Service lit à implémenter
import {createSlice, PayloadAction} from "@reduxjs/toolkit";

const litSlice = createSlice({

  name: 'lit',
  initialState: { },
  reducers: {

  },
});

export const { } = litSlice.actions

export default litSlice.reducer
