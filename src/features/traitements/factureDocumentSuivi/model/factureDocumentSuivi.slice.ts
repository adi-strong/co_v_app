// Service factureDocumentSuivi à implémenter
import {createSlice, PayloadAction} from "@reduxjs/toolkit";

const factureDocumentSuiviSlice = createSlice({

  name: 'factureDocumentSuivi',
  initialState: { },
  reducers: {

  },
});

export const { } = factureDocumentSuiviSlice.actions

export default factureDocumentSuiviSlice.reducer
