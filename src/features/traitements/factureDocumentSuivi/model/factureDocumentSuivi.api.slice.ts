// Service factureDocumentSuivi à implémenter
import {API, API_PATH} from "../../../app/store";

const factureDocumentSuiviApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = factureDocumentSuiviApiSlice
