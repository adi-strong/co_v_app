// Service documentSuivi à implémenter
import {createSlice, PayloadAction} from "@reduxjs/toolkit";

const documentSuiviSlice = createSlice({

  name: 'documentSuivi',
  initialState: { },
  reducers: {

  },
});

export const { } = documentSuiviSlice.actions

export default documentSuiviSlice.reducer
