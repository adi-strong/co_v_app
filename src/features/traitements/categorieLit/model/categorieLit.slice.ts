// Service categorieLit à implémenter
import {createSlice, PayloadAction} from "@reduxjs/toolkit";

const categorieLitSlice = createSlice({

  name: 'categorieLit',
  initialState: { },
  reducers: {

  },
});

export const { } = categorieLitSlice.actions

export default categorieLitSlice.reducer
