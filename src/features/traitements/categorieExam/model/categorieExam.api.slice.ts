// Service categorieExam à implémenter
import {API, API_PATH} from "../../../app/store";

const categorieExamApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = categorieExamApiSlice
