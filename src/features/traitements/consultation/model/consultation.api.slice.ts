// Service consultation à implémenter
import {API, API_PATH} from "../../../app/store";

const consultationApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = consultationApiSlice
