// Service prescription à implémenter
import {API, API_PATH} from "../../../app/store";

const prescriptionApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = prescriptionApiSlice
