// Service typeDocSuivi à implémenter
import {API, API_PATH} from "../../../app/store";

const typeDocSuiviApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = typeDocSuiviApiSlice
