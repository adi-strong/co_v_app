// Service factureConsultation à implémenter
import {createSlice, PayloadAction} from "@reduxjs/toolkit";

const factureConsultationSlice = createSlice({

  name: 'factureConsultation',
  initialState: { },
  reducers: {

  },
});

export const { } = factureConsultationSlice.actions

export default factureConsultationSlice.reducer
