// Service factureConsultation à implémenter
import {API, API_PATH} from "../../../app/store";

const factureConsultationApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = factureConsultationApiSlice
