// Service typeConsultation à implémenter
import {API, API_PATH} from "../../../app/store";

const typeConsultationApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = typeConsultationApiSlice
