// Service examen à implémenter
import {createSlice, PayloadAction} from "@reduxjs/toolkit";

const examenSlice = createSlice({

  name: 'examen',
  initialState: { },
  reducers: {

  },
});

export const { } = examenSlice.actions

export default examenSlice.reducer
