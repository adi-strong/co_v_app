// Service examen à implémenter
import {API, API_PATH} from "../../../app/store";

const examenApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = examenApiSlice
