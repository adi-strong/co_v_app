// Service hospitalisation à implémenter
import {API, API_PATH} from "../../../app/store";

const hospitalisationApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = hospitalisationApiSlice
