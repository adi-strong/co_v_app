// Service traitement à implémenter
import {API, API_PATH} from "../../../app/store";

const traitementApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = traitementApiSlice
