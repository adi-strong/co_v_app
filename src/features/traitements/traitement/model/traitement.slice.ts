// Service traitement à implémenter
import {createSlice, PayloadAction} from "@reduxjs/toolkit";

const traitementSlice = createSlice({

  name: 'traitement',
  initialState: { },
  reducers: {

  },
});

export const { } = traitementSlice.actions

export default traitementSlice.reducer
