// Service lab à implémenter
import {createSlice, PayloadAction} from "@reduxjs/toolkit";

const labSlice = createSlice({

  name: 'lab',
  initialState: { },
  reducers: {

  },
});

export const { } = labSlice.actions

export default labSlice.reducer
