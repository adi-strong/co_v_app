// Service rendezVous à implémenter
import {API, API_PATH} from "../../../app/store";

const rendezVousApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = rendezVousApiSlice
