// Service rendezVous à implémenter
import {createSlice, PayloadAction} from "@reduxjs/toolkit";

const rendezVousSlice = createSlice({

  name: 'rendezVous',
  initialState: { },
  reducers: {

  },
});

export const { } = rendezVousSlice.actions

export default rendezVousSlice.reducer
