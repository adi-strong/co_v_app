// Service infosGen à implémenter
import {createSlice, PayloadAction} from "@reduxjs/toolkit";

const infosGenSlice = createSlice({

  name: 'infosGen',
  initialState: { },
  reducers: {

  },
});

export const { } = infosGenSlice.actions

export default infosGenSlice.reducer
