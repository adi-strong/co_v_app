// Service infosGen à implémenter
import {API, API_PATH} from "../../../app/store";

const infosGenApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = infosGenApiSlice
