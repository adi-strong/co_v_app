// Service categorieLit à implémenter
import {API, API_PATH} from "../../../app/store";

const categorieLitApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = categorieLitApiSlice
