// Service uniteConsommation à implémenter
import {API, API_PATH} from "../../../app/store";

const uniteConsommationApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = uniteConsommationApiSlice
