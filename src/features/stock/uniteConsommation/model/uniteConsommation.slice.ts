// Service uniteConsommation à implémenter
import {createSlice, PayloadAction} from "@reduxjs/toolkit";

const uniteConsommationSlice = createSlice({

  name: 'uniteConsommation',
  initialState: { },
  reducers: {

  },
});

export const { } = uniteConsommationSlice.actions

export default uniteConsommationSlice.reducer
