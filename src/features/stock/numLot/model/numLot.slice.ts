// Service numLot à implémenter
import {createSlice, PayloadAction} from "@reduxjs/toolkit";

const numLotSlice = createSlice({

  name: 'numLot',
  initialState: { },
  reducers: {

  },
});

export const { } = numLotSlice.actions

export default numLotSlice.reducer
