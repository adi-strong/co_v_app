// Service numLot à implémenter
import {API, API_PATH} from "../../../app/store";

const numLotApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = numLotApiSlice
