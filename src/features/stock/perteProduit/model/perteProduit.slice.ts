// Service perteProduit à implémenter
import {createSlice, PayloadAction} from "@reduxjs/toolkit";

const perteProduitSlice = createSlice({

  name: 'perteProduit',
  initialState: { },
  reducers: {

  },
});

export const { } = perteProduitSlice.actions

export default perteProduitSlice.reducer
