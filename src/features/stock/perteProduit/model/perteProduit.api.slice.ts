// Service perteProduit à implémenter
import {API, API_PATH} from "../../../app/store";

const perteProduitApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = perteProduitApiSlice
