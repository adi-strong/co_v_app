// Service produit à implémenter
import {API, API_PATH} from "../../../app/store";

const produitApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = produitApiSlice
