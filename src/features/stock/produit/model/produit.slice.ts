// Service produit à implémenter
import {createSlice, PayloadAction} from "@reduxjs/toolkit";

const produitSlice = createSlice({

  name: 'produit',
  initialState: { },
  reducers: {

  },
});

export const { } = produitSlice.actions

export default produitSlice.reducer
