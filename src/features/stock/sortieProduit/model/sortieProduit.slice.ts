// Service sortieProduit à implémenter
import {createSlice, PayloadAction} from "@reduxjs/toolkit";

const sortieProduitSlice = createSlice({

  name: 'sortieProduit',
  initialState: { },
  reducers: {

  },
});

export const { } = sortieProduitSlice.actions

export default sortieProduitSlice.reducer
