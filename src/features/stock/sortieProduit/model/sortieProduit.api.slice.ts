// Service sortieProduit à implémenter
import {API, API_PATH} from "../../../app/store";

const sortieProduitApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = sortieProduitApiSlice
