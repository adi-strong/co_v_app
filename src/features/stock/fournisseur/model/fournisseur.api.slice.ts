// Service fournisseur à implémenter
import {API, API_PATH} from "../../../app/store";

const fournisseurApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = fournisseurApiSlice
