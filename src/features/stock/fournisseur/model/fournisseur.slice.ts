// Service fournisseur à implémenter
import {createSlice, PayloadAction} from "@reduxjs/toolkit";

const fournisseurSlice = createSlice({

  name: 'fournisseur',
  initialState: { },
  reducers: {

  },
});

export const { } = fournisseurSlice.actions

export default fournisseurSlice.reducer
