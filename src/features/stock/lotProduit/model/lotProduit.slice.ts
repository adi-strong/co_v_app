// Service lotProduit à implémenter
import {createSlice, PayloadAction} from "@reduxjs/toolkit";

const lotProduitSlice = createSlice({

  name: 'lotProduit',
  initialState: { },
  reducers: {

  },
});

export const { } = lotProduitSlice.actions

export default lotProduitSlice.reducer
