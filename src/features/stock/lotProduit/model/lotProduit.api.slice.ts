// Service lotProduit à implémenter
import {API, API_PATH} from "../../../app/store";

const lotProduitApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = lotProduitApiSlice
