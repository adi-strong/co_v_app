// Service factureProduit à implémenter
import {API, API_PATH} from "../../../app/store";

const factureProduitApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = factureProduitApiSlice
