// Service factureProduit à implémenter
import {createSlice, PayloadAction} from "@reduxjs/toolkit";

const factureProduitSlice = createSlice({

  name: 'factureProduit',
  initialState: { },
  reducers: {

  },
});

export const { } = factureProduitSlice.actions

export default factureProduitSlice.reducer
