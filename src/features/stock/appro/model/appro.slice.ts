// Service appro à implémenter
import {createSlice, PayloadAction} from "@reduxjs/toolkit";

const approSlice = createSlice({

  name: 'appro',
  initialState: { },
  reducers: {

  },
});

export const { } = approSlice.actions

export default approSlice.reducer
