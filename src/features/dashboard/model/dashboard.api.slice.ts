// Service dashboard à implémenter
import {API, API_PATH} from "../../../app/store";

const dashboardApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = dashboardApiSlice
