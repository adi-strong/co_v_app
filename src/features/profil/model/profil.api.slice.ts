// Service profil à implémenter
import {API, API_PATH} from "../../../app/store";

const profilApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = profilApiSlice
