// Service reception à implémenter
import {API, API_PATH} from "../../../app/store";

const receptionApiSlice = API.injectEndpoints({

  endpoints: build => ({

  })
  
})

export const { } = receptionApiSlice
